<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyMovies"
        tools:targetApi="31">

        <!-- Declare the MainActivity as the launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declare the EditMovie activity -->
        <activity
            android:name="com.myapplicationdev.android.mymovies.EditMovie"
            android:label="Edit Movie"
            />

        <!-- Declare the MovieList activity -->
        <activity
            android:name="com.myapplicationdev.android.mymovies.MovieList"
            android:label="Movie List"
             />

    </application>

</manifest>